# 🎓 DSU Grade API Service
* * *
###### API Service for the DSU Grade infrastructure.

## ⚙️ Configuration
You need to fill in the `.env` file using the example from `env.example`.
```bash
cp env.example .env
```

## 🏗️ Building
### Building / initialize container
```bash
make build
```

### Rebuilding / update app
```bash
make rebuild
```
